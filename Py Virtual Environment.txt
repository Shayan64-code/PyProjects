For making env:
python -m venv myvenv

For windows:(Activation)(Powershell VSCode)
.\pyvEn\Scripts\activate

For getting Requirements of Project:
pip freeze > requirements.txt

For Downloading through requirement file:
pip install -r requirements.txt

Otherwise:
pip install fastapi           /etc

for finding module:
streamlit --version

for listing all modules:
pip list

for checking the path of module:
pip show streamlit

for getting path of environment
Get-Command python
Get-Command pip
python -V
pip --version

